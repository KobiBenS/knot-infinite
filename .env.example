# LOCAL TESTING ONLY - Not needed in RunPod deployment
# RUNPOD_API_KEY=your_runpod_api_key_here  # Only for local CLI/testing

# S3 Storage Configuration (Optional - for output storage)
BUCKET_ENDPOINT_URL=https://s3.amazonaws.com
BUCKET_ACCESS_KEY_ID=your_access_key_id
BUCKET_SECRET_ACCESS_KEY=your_secret_access_key
BUCKET_NAME=infinitetalk-outputs

# These are set automatically by the Dockerfile
# MODEL_DIR=/workspace/models
# OUTPUT_DIR=/runpod-volume/outputs